use crate::controller::preset_event::{InsertPresetEvent, PresetEvent, UpdatePresetEvent};

generic_all!(PresetEvent);
generic_get!(PresetEvent);
generic_post!(PresetEvent);
generic_patch!(PresetEvent);
generic_delete!(PresetEvent);
